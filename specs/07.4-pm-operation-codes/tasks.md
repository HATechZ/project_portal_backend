# Tasks: PM & Operation Document Codes

- [x] Implement the approved shared persistence and authorization reuse for `src/pm-operation-code`.
      VERIFY: corepack yarn test --runInBand --testPathPatterns=pm-operation-code
- [x] Implement independent CRUD/lifecycle and idempotent starter provisioning.
      VERIFY: corepack yarn test --runInBand --testPathPatterns=pm-operation-code
- [x] Prove isolation, no range rejection and document integration compatibility.
      VERIFY: corepack yarn test --runInBand --testPathPatterns=pm-operation-code
