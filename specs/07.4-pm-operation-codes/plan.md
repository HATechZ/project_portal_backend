# Plan: PM & Operation Document Codes

The independent module at `src/pm-operation-code` reuses shared persistence and authorization, with provisioning, Swagger and compatibility/isolation tests without numeric classification.
