# API Contract: PM & Operation Document Codes

Future base route: `/api/v1/project-management-operation-document-codes`; independent list/detail/create/update/deactivate/reactivate operations follow platform envelopes/errors as a reference. Generic normalized code/name/optional description only; numeric value does not determine ownership. Authorization temporarily reuses `MANAGE_GENERAL_DOCUMENT_CODES` through the current pattern. Future Swagger display naming is `PM & Operation Document Codes`; this naming cleanup does not change the API route.
