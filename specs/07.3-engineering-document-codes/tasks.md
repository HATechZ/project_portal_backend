# Tasks

- [x] Implement the approved shared persistence and authorization reuse.
      VERIFY: corepack yarn test engineering-document-code --runInBand
- [x] Implement independent Engineering CRUD/lifecycle and idempotent starter provisioning.
      VERIFY: corepack yarn test engineering-document-code --runInBand
- [x] Prove all paired codes are distinct, no numeric range rejection, isolation and document compatibility.
      VERIFY: corepack yarn test engineering-document-code --runInBand
